import createMuiStrictModeTheme from '@material-ui/core/styles/createMuiStrictModeTheme';

const theme = createMuiStrictModeTheme({
  spacing: 4,
  palette: {
    primary: {
      main: '#7159c1',
    },
    secondary: {
      main: '#ca4949',
    },
  },
});

export default theme;
