import makeStyles from '@material-ui/styles/makeStyles';

export default makeStyles(() => ({
  form: {
    marginTop: 15,
    display: 'flex',
    width: 320,
    flexDirection: 'column',
  },
  input: {
    marginBottom: 5,
  },
}));
